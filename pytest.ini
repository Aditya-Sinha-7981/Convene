[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    model: needs real model weights; excluded from the default offline run (use -m "not model")
addopts = -m "not model"
